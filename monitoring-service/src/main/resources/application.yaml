server:
  port: 8085
spring:
  application:
    name: monitoring-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/rainiq_monitoring}
    username:  ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:rainiq123}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui/index.html
jwt:
  secret: ${JWT_SECRET:thisIsAVeryLongSecretKeyForRainIQProjectThatIsAtLeast256BitsLong}
design:
  service:
    url: http://localhost:8081
internal:
  service:
    token: eyJhbGciOiJIUzM4NCIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJpbnRlcm5hbEByYWluaXEuY29tIiwiaWF0IjoxNzg2ODEzNDY3LCJleHAiOjE4NDk4ODU0NjcsInJvbGUiOiJJTlRFUk5BTF9TRVJWSUNFIn0.PfU0Lkj7sPBIiqtngBoJhfZKqo2kDi0H7VK6RJ5yaKW6s96BL2YNKH__UqHESsTn
eureka:
  client:
    service-url:
      defaultZone:   ${EUREKA_URI:http://localhost:8761/eureka}
management:
  endpoint:
    health:
      show-details: never
  endpoints:
    web:
      exposure:
        include: health, prometheus